version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - misspell
  settings:
    misspell:
      locale: US

formatters:
  enable:
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/danjdewhurst/envio
